import { Layout } from 'antd';
import Header from './components/Header';

const App = () => {
  const { Header } = Layout;

  return (
    <Layout>
      <Header />
    </Layout>
  );
};

export default App;
